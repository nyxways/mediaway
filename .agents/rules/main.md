---
trigger: always_on
---

# Mediaway entrypoint

All workspace rules and system knowledge live in the sources below.
**Before any work** (code, investigation, Q&A, planning), read:

1. If helping a **contributor** (or unsure): `docs/contributing/for-agents.md` first
2. `docs/ai/wiki/index.md` — knowledge wiki. **Rule 0: read first.** Also open related pages.
3. Root `AGENTS.md` — absolute rules, behavior, language policy.
4. Optional: `local/` for this-machine scratch only (gitignored; not SSOT).

Edit rules only in `AGENTS.md`. **This file is a pointer — do not copy rules here.**

**Language:** speak to the user in their language; write all repo artifacts in English.
