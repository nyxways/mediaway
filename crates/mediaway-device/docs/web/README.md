# mediaway-device-web

Browser capture bindings (`getUserMedia` / `getDisplayMedia`) for wasm32.

**Policy:** no programmatic device/window selection — see [`adr/0001-web-capture-policy.md`](adr/0001-web-capture-policy.md).
