# mediaway-encoder — docs

HW encode **facade** (traits + `auto` types). Windows: `WindowsVideoEncoder::open` / `AutoVideoEncoder::open`.  
Platform order: **Windows → Web → Linux → other**. Future umbrella: `mediaway-codec`.

Support tables: root [`README.md`](../../README.md#codec-support).

| Doc | Notes |
|-----|-------|
| [roadmap.md](docs/roadmap.md) | Facade + platform crate stages |
| [adr/](adr/) | Traits; auto encode; backends hold OS ADRs |
