# Bun + TypeScript scripts

Canonical: [`docs/conventions/scripts.md`](../../../conventions/scripts.md) · `tools/scripts/`.

- Light maintainers’ utils → Bun + TS
- Product / library / `mediaway-*-cli` → Rust
- Lefthook hooks → bash (`tools/hooks/`)
- No new Node/Python shared script trees
