# Pinned (not `stable`) so local hooks and CI lint with the same clippy.
# Bump together with every `dtolnay/rust-toolchain@<ver>` in .github/workflows/.
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy", "rust-src"]
profile = "default"
